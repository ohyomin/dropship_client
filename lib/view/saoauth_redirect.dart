import 'package:flutter/material.dart';

class SARedirectPage extends StatelessWidget {
  const SARedirectPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("");
  }
}
